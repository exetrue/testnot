name: notlaan_secure_notes
version: 1.0.0+1
description: Makrosoft NOTLAAN secure notes, reminders and password vault.
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.3
  crypto: ^3.0.3
  intl: ^0.19.0

flutter:
  uses-material-design: true
  assets:
    - assets/app_icon.png
